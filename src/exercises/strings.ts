/**
 * Return a reversed string.
 * Example: reverseString("abc") -> "cba"
 */
export function reverseString(value: string): string {
  // TODO: Implement this.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Check whether a string is a palindrome exactly as typed.
 * Example: isPalindrome("level") -> true
 * Example: isPalindrome("Level") -> false
 */
export function isPalindrome(value: string): boolean {
  // TODO: Implement this.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Count how many times each character appears.
 * Example: countCharacters("banana") -> Map { "b" => 1, "a" => 3, "n" => 2 }
 */
export function countCharacters(value: string): Map<string, number> {
  // TODO: Implement this with Map.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Return the first character that appears exactly once.
 * Example: firstUniqueCharacter("leetcode") -> "l"
 * Example: firstUniqueCharacter("aabb") -> null
 */
export function firstUniqueCharacter(value: string): string | null {
  // TODO: Implement this.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Check whether two strings are anagrams with exact case-sensitive characters.
 * Example: isValidAnagram("listen", "silent") -> true
 * Example: isValidAnagram("Listen", "silent") -> false
 */
export function isValidAnagram(a: string, b: string): boolean {
  // TODO: Implement this with a frequency counter.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Return the longest common prefix shared by all words.
 * Example: longestCommonPrefix(["flower", "flow", "flight"]) -> "fl"
 * Example: longestCommonPrefix([]) -> ""
 */
export function longestCommonPrefix(words: string[]): string {
  // TODO: Implement this.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}
