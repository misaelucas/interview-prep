/**
 * Find the largest number in an array.
 * Example: findMax([3, 1, 9, 2]) -> 9
 * Example: findMax([]) -> null
 */
export function findMax(numbers: number[]): number | null {
  // TODO: Implement this.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Find the smallest number in an array.
 * Example: findMin([3, 1, 9, 2]) -> 1
 * Example: findMin([]) -> null
 */
export function findMin(numbers: number[]): number | null {
  // TODO: Implement this.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Return a new array with items in reverse order.
 * Example: reverseArray([1, 2, 3]) -> [3, 2, 1]
 */
export function reverseArray<T>(items: T[]): T[] {
  // TODO: Implement this without mutating the input array.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Return a new array with duplicates removed, keeping the first occurrence order.
 * Example: removeDuplicates([1, 2, 2, 3, 1]) -> [1, 2, 3]
 */
export function removeDuplicates<T>(items: T[]): T[] {
  // TODO: Implement this.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Detect whether an array has duplicates using a brute force nested-loop approach.
 * Example: hasDuplicateBruteForce([1, 2, 3]) -> false
 * Example: hasDuplicateBruteForce([1, 2, 1]) -> true
 */
export function hasDuplicateBruteForce<T>(items: T[]): boolean {
  // TODO: Implement this with nested loops first.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Detect whether an array has duplicates using an optimized lookup structure.
 * Example: hasDuplicateOptimized([1, 2, 3]) -> false
 * Example: hasDuplicateOptimized([1, 2, 1]) -> true
 */
export function hasDuplicateOptimized<T>(items: T[]): boolean {
  // TODO: Implement this with Set.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Merge two already sorted arrays into one sorted array.
 * Example: mergeSortedArrays([1, 3], [2, 4]) -> [1, 2, 3, 4]
 */
export function mergeSortedArrays(a: number[], b: number[]): number[] {
  // TODO: Implement this with two pointers.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Return a new array with all zeros moved to the end.
 * Example: moveZerosToEnd([0, 1, 0, 3, 12]) -> [1, 3, 12, 0, 0]
 */
export function moveZerosToEnd(numbers: number[]): number[] {
  // TODO: Implement this without changing the relative order of non-zero numbers.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}

/**
 * Rotate an array to the right by k steps.
 * Example: rotateArray([1, 2, 3, 4, 5], 2) -> [4, 5, 1, 2, 3]
 * Example: rotateArray([], 3) -> []
 */
export function rotateArray(numbers: number[], k: number): number[] {
  // TODO: Implement this and handle k larger than the array length.
  // Time complexity:
  // Space complexity:
  throw new Error("TODO");
}
